<% layout('/layouts/boilerplate') -%>

  <body>
    <!-- <h3>Listings</h3> -->
    <!-- <form method="get" action="/listings/new">
      <button>Create new</button>
  </form> -->

    <!-- <div id="filters mt-3">
      <div class="filter">
        <i class="bi bi-fire"></i><i class="bi bi-compass"></i>
        <p>Trending</p>
      </div>
    </div> -->
    <div class="row justify-content-around pb-3 row-cols-3 mt-3">
      <%for(data of initData){%>
        <a href="/listings/<%= data._id %>/show">
          <div class="card index-img listing-card" style="width: 18rem">
            <img src="<%= data.image.url %>" class=" card-img-top" alt="listing image" />
            <div class="card-img-overlay"></div>
            <div class="card-body">
              <p class="card-text">
                <%= data.title %>
              </p>
              <p>&#8377;<%= data.price.toLocaleString("en-IN") %> / night</p>
            </div>
          </div>
        </a>
        <%}%>
    </div>
  </body>